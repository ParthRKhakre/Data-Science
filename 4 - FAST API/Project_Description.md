# PresDoc API 
> So the problem is when we visit doctor we need to have a prescription I want to built a simple platform so that it can be easy for patient to maintain a record.

# EndPoints
* /create - add patient 
* /view - view patient
* /view/patient_id
* /update/patient_id
* /delete/patient_id